import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-login-page-unused',
  standalone: true,
  imports: [CommonModule],
  template: `<!-- This component is created to fix a file error but is not used in the application -->`
})
export class LoginPageUnusedComponent {
  constructor() { }
}
